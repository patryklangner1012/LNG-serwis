---
---
<header class="nav-wrap">
  <nav class="nav">
    <a href="/" class="nav-logo" aria-label="LNG Best Partner Serwis">
      <!-- Logo z wizytówki: niebieski okrąg LNG + BEST PARTNER Sp. z o.o. -->
      <svg viewBox="0 0 440 120" xmlns="http://www.w3.org/2000/svg" height="38" width="auto">
        <!-- Niebieski okrąg -->
        <circle cx="60" cy="60" r="58" fill="#00A0E3"/>
        <!-- LNG w okręgu -->
        <text x="60" y="50" font-family="Arial Black,sans-serif" font-size="26" font-weight="900" fill="#fff" text-anchor="middle" font-style="italic">LNG</text>
        <rect x="14" y="57" width="92" height="2.5" fill="#fff" rx="1"/>
        <text x="60" y="84" font-family="Arial,sans-serif" font-size="11" font-weight="700" fill="#fff" text-anchor="middle" letter-spacing="1.5">BEST PARTNER</text>
        <!-- Tekst po prawej -->
        <text x="138" y="58" font-family="Arial Black,sans-serif" font-size="38" font-weight="900" fill="#0a0a14" letter-spacing="-0.5">BEST PARTNER</text>
        <text x="140" y="92" font-family="Arial,sans-serif" font-size="22" font-weight="600" fill="#0a0a14" font-style="italic">Sp. z o.o.</text>
      </svg>
      <span class="nav-divider"></span>
      <span class="nav-subtitle">Serwis kriogeniczny</span>
    </a>

    <div class="nav-links" id="nav-links">
      <a href="/#uslugi">Usługi</a>
      <a href="/#technologia">Technologia</a>
      <a href="/#galeria">Galeria</a>
      <a href="/aktualnosci">Aktualności</a>
      <a href="https://transport-lng.pl" target="_blank" rel="noopener" class="nav-tr">↗ Transport LNG</a>
      <a href="/#kontakt" class="nav-ct">Kontakt</a>
    </div>

    <button class="nav-burger" id="burger" aria-label="Menu" aria-expanded="false">
      <span></span><span></span><span></span>
    </button>
  </nav>
</header>

<script>
  const burger = document.getElementById('burger');
  const links  = document.getElementById('nav-links');
  burger?.addEventListener('click', () => {
    const open = links?.classList.toggle('open');
    burger.setAttribute('aria-expanded', String(!!open));
  });
</script>

<style>
.nav-wrap {
  position: sticky; top: 0; z-index: 100;
  background: #fff; border-bottom: 1px solid #f0f0f0;
}
.nav {
  display: flex; align-items: center; justify-content: space-between;
  padding: 0 6%; height: 72px; max-width: 1600px; margin: 0 auto;
}
.nav-logo {
  display: flex; align-items: center; gap: 0;
  text-decoration: none; flex-shrink: 0;
}
.nav-divider {
  display: inline-block; width: 1px; height: 30px;
  background: #e8e8e8; margin: 0 18px;
}
.nav-subtitle {
  font-size: 11px; font-weight: 600; letter-spacing: .1em;
  text-transform: uppercase; color: #b0b8c4; white-space: nowrap;
}
.nav-links {
  display: flex; gap: 28px; align-items: center;
}
.nav-links a {
  font-size: 13px; color: #555; font-weight: 500;
  text-decoration: none; transition: color .15s; white-space: nowrap;
}
.nav-links a:hover { color: #00A0E3; }
.nav-tr {
  border: 1px solid rgba(0,160,227,.3); color: #00A0E3 !important;
  padding: 6px 16px; border-radius: 6px; font-size: 12px !important; font-weight: 700 !important;
}
.nav-ct {
  background: #0a0a14 !important; color: #fff !important;
  padding: 10px 22px; border-radius: 8px;
  font-weight: 700 !important; font-size: 13px !important;
}
.nav-ct:hover { opacity: .85; }
.nav-burger {
  display: none; flex-direction: column; gap: 5px;
  background: none; border: none; cursor: pointer; padding: 6px; flex-shrink: 0;
}
.nav-burger span { display: block; width: 22px; height: 2px; background: #0a0a14; border-radius: 2px; }

@media (max-width: 960px) {
  .nav { padding: 0 5%; }
  .nav-subtitle { display: none; }
  .nav-divider { display: none; }
  .nav-burger { display: flex; }
  .nav-links {
    display: none; position: absolute; top: 72px; left: 0; right: 0;
    background: #fff; border-bottom: 1px solid #f0f0f0;
    flex-direction: column; align-items: flex-start;
    padding: 20px 5%; gap: 18px;
    box-shadow: 0 8px 24px rgba(0,0,0,.08);
  }
  .nav-links.open { display: flex; }
  .nav-links a { font-size: 15px; width: 100%; }
}
</style>
